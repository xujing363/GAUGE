# Optional Heavy Dependencies

The following flows are preserved but treated as optional in default smoke validation:

- `targets/DrugDesign_Sec/cnm/validation/`
  - GNINA binaries
  - GTEx V11
  - LINCS datasets
  - Additional TCGA/repurposing resources

These are kept for full reproducibility, but not required for the default entry-level smoke pass.
