# KG Publication Bundle

This bundle is prepared from `/mnt/raid5/xujing/KG` without modifying or deleting original content.

See:
- `docs/README.md` for structure and usage
- `runtime/` for one-command entry scripts
- `snapshots/` for linked heavy result directories
- `runtime/smoke_report.json` for latest smoke-check result
